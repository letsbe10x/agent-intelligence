"""Built-in agents shipped with the framework.

Each agent is self-contained in its own subpackage with:
    - agent.py        : Agent subclass implementation
    - schemas.py      : Input/Output/Params pydantic models
    - prompts/*.md    : Externalised prompt templates
    - config.yaml     : Default config (optional starting point)
"""
